# API Reference

Saint CMS automatically generates highly optimized REST endpoints for every collection you define in your `saint.config.json` file. 

You do not need to write any backend routing logic. The Go engine handles it all.

## Base URL
When running locally, your API is available at:
`http://localhost:3333/api/v1`

---

## Fetching Data

To fetch data from a collection, you simply hit the endpoint matching your collection's name. 

### GET /collections/{name}
Retrieves a list of all records in a specific collection.

**Example Request:**
```javascript
// Fetching all products in a Next.js Server Component
const res = await fetch('http://localhost:3333/api/v1/collections/product');
const data = await res.json();

{
  "status": "success",
  "data": [
    {
      "id": 1,
      "title": "Mechanical Keyboard",
      "price": 120,
      "in_stock": true,
      "created_at": "2026-06-25T14:35:17Z"
    }
  ]
}